import { renderProjectSelector } from './projectSelector.interface.js';

export function initProjectSelector(container, onSelect) {
    console.log("[PROJECT SELECTOR] init");
    renderProjectSelector(container, onSelect);
}
